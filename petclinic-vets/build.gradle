ext.artifactId = 'petclinic-vets'
group = 'com.github.allen.parslow'
ext.webAppPort = 8082

apply from: "${gradleScriptsDir}/standard-webapp.gradle"

dependencies {
    compile project(':petclinic-api-core')
    testCompile project(':petclinic-api-testing')
}
